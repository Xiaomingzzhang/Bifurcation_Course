# Neimark-Sacker 分岔

Neimark-Sacker (NS) 分岔有时也称为是映射的 Hopf 分岔, 与 Hopf 分岔类似, 系统的不动点失稳, 而分岔出一个不变圈. 粗略地讲, 映射的 NS 分岔现象相应于微分方程系统从周期解分岔出拟周期解.

考虑平面映射
$$
(x,y)^T\mapsto R(\omega)(x,y)^T,
$$
其中
$$
R(\phi)=\begin{bmatrix}
    \cos\phi&-\sin\phi\\
    \sin\phi&\cos\phi
\end{bmatrix}
$$
为逆时针旋转的旋转矩阵. 在此映射下, $(0,0)^T$ 为其不动点, 而任何一个以原点为圆心的圆都是这个映射的不变集. 取决于 $\omega$ 的取值, 当限制在不变圆上时, 如果 $\omega$ 与 $2\pi$ 是可以通约的, 即 $\frac{\omega}{2\pi}$ 是有理数, 那么轨道都是周期轨道; 如果 $\frac{\omega}{2\pi}$ 是无理数, 那么轨道将在不变圆上稠密.

这个线性旋转映射的 Jacobi 矩阵的特征值为 $e^{\pm i\omega}$, 其位于单位圆上. 那么如果非线性系统的线性化映射也满足这个性质, 那么就可能产生不变圈.

考虑极坐标 $(r,\phi)$ 下的平面映射:
$$
\label{ns_two_d}
r'&=r(1+\mu+a(\mu)r^2),\\
\phi'&=\phi+\theta(\mu),
$$
其中 $a(\mu),\theta(\mu)$ 为关于参数 $\mu$ 的光滑函数. 上述映射在两个坐标方向是相互不耦合的. 单独考虑映射:
$$
\label{ns_one_d}
r'=r(1+\mu+a(\mu)r^2),
$$
那么该映射的不动点对应于原系统的一个不变圆:
- $r=0$ 为映射 [](#ns_one_d) 的不动点, 对应于原点;
- 若 $a(0)<0$, 当 $\mu>0$, [](#ns_one_d) 具有稳定的不动点 $r_0(\mu)=\sqrt{-\mu/a(\mu)}$, 同时不动点 $r=0$ 失稳.

[](#ns_two_d) 的第二式代表在圆周上的一个映射. 当限制在不变圆上时, 如果 $\theta(\mu)$ 与 $2\pi$ 是可以通约的, 即 $\frac{\theta(\mu)}{2\pi}$ 是有理数, 那么轨道都是周期轨道; 如果 $\frac{\theta(\mu)}{2\pi}$ 是无理数, 那么轨道将在不变圆上稠密.

上述平面映射从一个不动点分岔出一个不变圈的现象称为 NS 分岔.

如果将映射 [](#ns_two_d) 返回到直角坐标, 我们将得到:
$$
x'&=r'\cos\phi'=(x\cos\theta(\mu)-y\sin\theta(\mu))(1+\mu+a(\mu)(x^2+y^2)),\\
y'&=r'\sin\phi'=(x\sin\theta(\mu)+y\cos\theta(\mu))(1+\mu+a(\mu)(x^2+y^2)).
$$
上述映射在原点处的 Jacobi 矩阵为: 
$$
(1+\mu)R(\theta(\mu)).
$$
因此其特征值为共轭复数 $\lambda(\mu),\bar{\lambda}(\mu)=(1+\mu)e^{\pm i\theta(\mu)}$.

在上述分析中, 原点在 $\mu=0$ 处分岔出不变圈 $r_0(\mu)=\sqrt{-\mu/a(\mu)}$, 此时 $|\lambda(0)|=1$ 位于复平面上的单位圆上; 而在 $\mu=0$ 附近, 特征值穿越了单位圆. 这个特征与 Hopf 分岔类似, 不过 Hopf 分岔的条件为特征值穿越虚轴.

## NS 分岔定理
这一小节, 我们给出一个平面映射发生 NS 分岔的充分条件.

考虑平面单参数映射:
$$
\label{ns_map}
x'=A(\mu)x+F(x,\mu),
$$
其中 $x=(x_1,x_2)^T\in\mathbb{R}^2$, 矩阵 $A(\mu)$ 和 $F(x,\mu)$ 都是关于 $\mu,x$ 的充分光滑的函数, 并且 $F(x,\mu)=O(||x||^2)$.

```{prf:assumption}
:label: nsa1
- $A(\mu)$ 具有一对共轭复特征值 $\lambda(\mu),\bar{\lambda}(\mu)=r(\mu)e^{\pm i \theta(\mu)}$, 其中 $r(0)=1,r'(0)>0$;
- 对于 $j=1,2,3,4$, $\lambda_0^j\neq 1$, 其中 $\lambda_0=\lambda(0)$.
```
[](#nsa1) 中的第一个条件为分岔条件与横截性条件, 它保证当参数 $\mu$ 在 0 附近变化时特征值能穿越复平面上的单位圆; 第二个条件为非共振条件, 它保证我们能消去足够多的低阶项, 其几何含义是: 相点在线性映射的作用下, 不能迭代较低阶次就回到自身.

```{prf:lemma}
对于映射 [](#ns_map), 如果 [](#nsa1) 成立, 那么在适当的坐标变化下, 映射 [](#ns_map) 在原点附近具有形式:
$$
z'=\lambda(\mu)z+c_{1}(\mu)z^2\bar{z}+O(|z|^4),
$$
其中 $z\in\mathbb{C}$ 为复数.
```

```{prf:proof}
:numbered: false
首先取变换 $x=P(\mu)y$ 使得映射在新坐标下为
$$
\label{ns_map_linear_transed}
y'=r(\mu)R(\theta(\mu))y+P^{-1}(\mu)F(P(\mu)y,\mu).
$$
$P(\mu)$ 的两列可分别取 $A(\mu)$ 的特征值 $\bar{\lambda}(\mu)$ 的特征向量的实部与虚部.

令 $z=y_1+i y_2,\bar{z}=y_1-iy_2$. 逆变换为 $y_1=\frac{z+\bar{z}}{2},y_2=\frac{z-\bar{z}}{2}$. [](#ns_map_linear_transed) 的第二式乘以 $i$ 再加上第一式, 得到:
$$
z'=\lambda(\mu)z+g(z,\bar{z},\mu).
$$
将函数 $g$ 在 $(z,\bar{z})=(0,0)$ 处展开, 得到:
$$
\label{ns_zmap}
z'=\lambda(\mu)z+g_2(z,\bar{z},\mu)+g_3(z,\bar{z},\mu)+\cdots,
$$
其中
$$
g_n(z,\bar{z},\mu)=\sum_{k+l=n}\frac{g_{kl}(\mu)}{k!l!}z^{k}\bar{z}^l.
$$
其中 
$$
g_{kl}(\mu)=\frac{\partial^n g}{\partial z^k \partial \bar{z}^l}(0,0,\mu).
$$

取变换：
$$
w=z+h_2(z,\bar{z},\mu),
$$
其中 $h_2$ 的形式与 $g_2$ 相同. 上述变换的逆变换为:
$$
z=w-h_2(w,\bar{w},\mu)+O(|w|^3).
$$
那么在新坐标下, 映射 [](#ns_zmap) 具有形式:
$$
w'&=z'+h_2(z',\bar{z}',\mu)\\
&=\lambda(\mu)z+g_2(z,\bar{z},\mu)+O(|z|^3)+h_2(\lambda(\mu)z+g_2(z,\bar{z},\mu)+O(|z|^3),\overline{\lambda(\mu)z+g_2(z,\bar{z},\mu)+O(|z|^3)},\mu)\\
&=\lambda(\mu)w-\lambda(\mu)h_2(w,\bar{w},\mu)+h_2(\lambda(\mu)w,\bar{\lambda}(\mu)\bar{w})+g_2(w,\bar{w},\mu)+O(|w|^3).
$$

为了消去二次项, 可取
$$
h_{kl}(\mu)=\frac{g_{kl}(\mu)}{\lambda(\mu)-\lambda^k(\mu)\bar{\lambda}^l(\mu)}\quad (k+l=2).
$$
为了保证上面的 $h_{kl}$ 在 $(z,\bar{z},\mu)=0$ 处是定义完好的, 只需要
$$
\lambda_0-\lambda_0^k\bar{\lambda}_0^l\neq 0.
$$
利用 $|\lambda_0|=1$, 上面的式子也可改写为:
$$
\lambda_0^{k-l-1}\neq 1 \Leftrightarrow  \lambda_0^{-k+l+1}\neq 1
$$

对于满足 $k+l=2$ 的所有非负整数对, $|k-l-1|\leq 4$ 且 $k-l-1\neq 0$. 利用假设 [](#nsa1), 所有的二次项均可消去. 那么变换以后, 映射的形式为:
$$
w'=\lambda(\mu)w+\hat{g}_{3}(w,\bar{w},\mu)+O(|w|^4).
$$
再取坐标变换
$$
q=w+h_3(w,\bar{w},\mu),
$$
类似地推导得到: 如果要消去三次项, 那么需取:
$$
h_{kl}(\mu)=\frac{\hat{g}_{kl}(\mu)}{\lambda(\mu)-\lambda^k(\mu)\bar{\lambda}^l(\mu)}\quad (k+l=3).
$$
但满足 $k+l=3$ 的非负整数对 $(2,1)$, $k-l-1= 0$. 并且, 这些整数对中, $|k-l-1|\leq 4$. 因此, 利用假设 [](#nsa1), 仅有 $q^2\bar{q}$ 这一项无法消去. 不妨令 $h_{kl}(\mu)=0$, 那么在新坐标下的映射为:
$$
q'&=\lambda(\mu)q+\hat{g}_{21}(\mu)q^2\bar{q}+O(|q|^4)\\
&=\lambda(\mu)q+c_1(\mu)q^2\bar{q}+O(|q|^4)
$$

可以看到, $c_1(\mu)$ 是第一次变换以后的三次项 $w^2\bar{w}$ 的系数. 其表达式为:
$$
c_1=\frac{1}{2}g_{21}+\frac{\bar{\lambda}|g_{02}|^2}{2(\lambda-\bar{\lambda}^2)}-\frac{g_{11} \left(g_{20} (3 |\lambda|^2 -2 \lambda -\bar{\lambda})+2\lambda (\lambda -1)   \bar{g}_{11}\right)}{2\lambda(\lambda-1)(\bar{\lambda}-1)},
$$
其中我们略去了变量对于 $\mu$ 的依赖性.
```

```{prf:remark}
- 如果要按照上述的流程来计算 $c_1(\mu)$, 必须要计算逆变换 $z=w-h_2(w,\bar{w},\mu)+O(|w|^3)$ 的三次项. 具体计算可参考 [](./61symbolic.md).
- 可以看到, $c_1(\mu)$ 只依赖于最开始的复映射 [](#ns_zmap), 而得到这个复映射需要作两次变换; 可参考 Hopf 分岔时介绍的投影法来通过原系统 [](#ns_map) 计算 $g_{ij}$.
- 有些教材上的 $c_1(\mu)$ 与我们所得到的不同, 然而, 由于在 $\mu=0$ 处 $|\lambda(0)|=1$, 利用这个条件, 可验证这些表达式在 $\mu=0$ 时是等价的.
```

```{prf:assumption}
:label: nsa2
- $a(0)=\text{Re}(\bar{\lambda}_0 c_1(0))\neq 0$.
```

```{prf:theorem} NS 分岔定理
考虑平面映射 [](#ns_map), 如果假设 [](#nsa1) 和 [](#nsa2) 成立, 那么当 $\mu$ 在 $0$ 附近变化时, 映射从原点分岔出一个不变圈, 且原点的稳定性与该不变圈的稳定性相异:
- 当 $a(0)<0$, 分岔出的不变圈是稳定的;
- 当 $a(0)>0$, 分岔出的不变圈是不稳定的.
```

首先, 当假设成立时, 在原点附近, 我们可以在合适的坐标下将原映射 [](#ns_map) 转变为:
$$
z'=\lambda(\mu)z+c_{1}(\mu)z^2\bar{z}+O(|z|^4).
$$
我们先来观察一下, 如果将四次及其以上的项省略, 会得到什么信息.

将复坐标变换到极坐标形式: $z=\rho e^{i\phi}$. 那么
$$
z'&=\rho'e^{i\phi'}\\
&=\lambda(\mu)z+c_{1}(\mu)z^2\bar{z}\\
&=r(\mu)e^{i\theta(\mu)}\rho e^{i\phi}+c_1(\mu)\rho^3 e^{i\phi}\\
&=\rho e^{i(\phi+\theta(\mu))}\big(r(\mu)+d(\mu)\rho^2\big)\\
&=\rho e^{i(\phi+\theta(\mu))}r(\mu)\big[(1+\frac{a(\mu)}{r(\mu)}\rho^2)+i\frac{b(\mu)}{r(\mu)}\rho^2\big]\\
&=\Big[\rho(r(\mu)+a(\mu)\rho^2)+O(\rho^4)\Big]e^{i(\phi+\theta(\mu)+O(\rho^2))},
$$
其中:
$$
d(\mu)=c_1(\mu)e^{-i\theta(\mu)},\quad a(\mu)=\text{Re}(d(\mu)),\quad b(\mu)=\text{Im}(d(\mu)),
$$
最后一步我们用到了公式: $x+iy=\sqrt{x^2+y^2}e^{i\arctan\frac{y}{x}}$.

因此, 我们得到极坐标下的映射:
$$
\rho'&=\rho(r(\mu)+a(\mu)\rho^2)+O(\rho^4),\\
\phi'&=\phi+\theta(\mu)+O(\rho^2).
$$
第一个式子与第二个式子不耦合, 因此我们得到一个半径为:
$$
\rho_0(\mu)=\sqrt{-\frac{r'(0)\mu}{a(0)}}+O(\mu)
$$
的不变圈. 在此不变圈上, 旋转映射为:
$$
\phi'=\phi+\theta(\mu)+O(\rho^2):=\phi+\omega(\mu).
$$

NS 分岔的完整证明需要用到压缩映射原理. 感兴趣的读者可参考 p194.

## 种群增长方程的 NS 分岔
考虑具有时间滞后的种群增长方程:
$$
N_{n+1}=\mu N_{n}(1-N_{n-1}),
$$
取 $x=N_{n-1},y=N_n$, 我们得到二维映射:
$$
x'&=y\\
y'&=\mu y(1-x).
$$
该映射有不动点:
$$
(0,0),\quad (\frac{\mu-1}{\mu},\frac{\mu-1}{\mu}). 
$$
映射在第二个不动点处的 Jacobi 矩阵具有特征值 $\lambda(\mu)=\sqrt{\mu-1}e^{i\arctan\sqrt{4\mu-5}}$.

当 $\mu=2$ 时, $|\lambda(2)|=1$ 且 $\lambda^j(2)\neq 1$ 对于 $j\leq 5$ 成立. 并且
$$
\frac{d}{d\mu}|\lambda(\mu)|_{\mu=2}=\frac{1}{2}.
$$
因此上述映射在不动点 $(\frac{\mu-1}{\mu},\frac{\mu-1}{\mu})$ 处, $\mu=2$ 时满足 NS 分岔的第一个条件 [](#nsa1). 

下面我们来验证 NS 分岔的第二个条件. 使用投影法来计算 $g_{kl}$. 令 $\mu=2$. 那么在不动点处的 Jacobi 矩阵为:

$$
A=
\begin{bmatrix}
    0 & 1\\
    -1 &1
\end{bmatrix}.
$$

$\lambda_0=\lambda(2)=\frac{1+i\sqrt{3}}{2}$. 那么满足
$$
Aq=\lambda_0q,\quad A^Tp=\bar{\lambda}_0p,\quad\langle p,q \rangle=1
$$
的 $q,p$ 矢量可取为:
$$
q=(\frac{1}{2} \left(1-i \sqrt{3}\right),1),\\
p=(\frac{\sqrt{3}-i}{\sqrt{3}+3 i},\frac{2 i}{\sqrt{3}+3 i}).
$$

由于没有三次项, 故映射的三次线性型为 0. 对于二次线性型 $B$, 我们有:
$$
B(\xi,\eta)=(0,-2\eta_2\xi_1-2\eta_1\xi_2).
$$
那么:
$$
g_{21}&=0,\\
g_{20}&=\langle p,B(q,q) \rangle=\frac{4 i}{\sqrt{3}},\\
g_{11}&=\langle p,B(q,\bar{q}) \rangle=-1+\frac{i}{\sqrt{3}},\\
g_{02}&=\langle p,B(\bar{q},\bar{q}) \rangle=-2-\frac{2 i}{\sqrt{3}}.
$$
最终得到:
$$
\text{Re}(\bar{\lambda}c_1(0))=-2<0.
$$

因此不动点在 $\mu=2$ 处产生了超临界的 NS 分岔.

```{prf:observation}
NS 分岔定理要求不动点始终固定在原点, 在这个例子的计算中, 不动点是随着参数移动的, 这样计算会改变 $c_1(0)$ 的取值吗?
```

